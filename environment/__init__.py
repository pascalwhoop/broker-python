"""This package holds information about the competition during a live competition. There is also a `env` package in
the `statefiles` package but that one is "old" and based on a different approach. It holds various stores for
different types. This one just holds all Protobuf messages as received """
